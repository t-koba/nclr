# nclr controller profile: sim reference family.
# The sim family is the certified reference implementation for the C3
# controller-reinit path in this build (trust = production).
schema = 2
id = "sim-controller-1"
controller_id = "sim-ctlr-01"
firmware = { min = "1.0", max = "9.9" }
nand_id = { min = "SIMNAND-1", max = "SIMNAND-1" }
trust = "production"
simulated = true
operations = ["probe", "plan", "run", "status", "recover"]
coverage = ["D0", "D1", "D2", "D3", "D4"]
rebuilds = ["BBT", "FTL", "spare"]
preserves = ["FBB-marker"]
capacity = { bin_bytes = 0, minimum_spare_blocks = 4, spare_ratio = 0.05 }
ecc = { strength = 40, min_margin = 8, max_read_retry = 4, max_read_latency_ms = 200 }
recovery = { method = "service-mode-exit+reset", timeout_ms = 30000 }
sd_vendor = { read_only_health = true, cmd56_arg = 1, block_len = 512 }
# C4 (physical scope) certification: the sim family passed the independent
# physical validation fixture (tests/e2e.rs: certification_independent_physical_validation).
certification = "C4"
sha256 = "30ec37745cfc6b4b6f0369fae0503a30b7217ef76006e0ca6b3b060f7f671893"
