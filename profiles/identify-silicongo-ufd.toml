# Candidate-only identification profile: silicongo (SG1580/81, KS6808, UD6809).
# The vendor-owned VID is not confirmed from primary sources (usb.ids has no
# unique registration), so no VID hint selects a command. Exact controller
# and NAND identities must come from an authenticated bootstrap recipe after
# the device's exact OS identity is captured with nclr info.
schema = 1
id = "silicongo-ufd"
family = "silicongo-ufd"
